[project]
name = "vehicle360-medallion"
version = "0.2.0"
description = "Vehicle 360 metadata-driven medallion framework for Databricks"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
